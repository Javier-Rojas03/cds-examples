@EndUserText.label: 'Access control'
@MappingRole: true
define role ZAC_EXAMPLE {
    grant 
        select
            on
                ZI_EXAMPLE
                    where
                        (Name) = aspect pfcg_auth(ZAO_EXAMP, ZPROD, ACTVT = '03' );
                        
}